{{- if .Values.logCollector.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: log-collector
  namespace: {{ include "ai-gateway.namespace" . }}
  labels: {{- include "ai-gateway.labels" . | nindent 4 }}
spec:
  {{- if .Values.logCollector.service.nodePort.enabled }}
  type: NodePort
  {{- else }}
  type: ClusterIP
  {{- end }}
  selector: {{- include "ai-gateway.selectorLabels" (dict "root" . "name" "log-collector") | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: http
      {{- if .Values.logCollector.service.nodePort.enabled }}
      nodePort: {{ .Values.logCollector.service.nodePort.port }}
      {{- end }}
{{- end }}
