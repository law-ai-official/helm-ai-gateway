{{- if .Values.newapi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: newapi
  namespace: {{ include "ai-gateway.namespace" . }}
  labels: {{- include "ai-gateway.labels" . | nindent 4 }}
spec:
  {{- if .Values.newapi.service.nodePort.enabled }}
  type: NodePort
  {{- else }}
  type: ClusterIP
  {{- end }}
  selector: {{- include "ai-gateway.selectorLabels" (dict "root" . "name" "newapi") | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: http
      {{- if .Values.newapi.service.nodePort.enabled }}
      nodePort: {{ .Values.newapi.service.nodePort.port }}
      {{- end }}
{{- end }}
